{% extends 'base.html.twig' %}


{% block title %}
    Edition de {{ category.name }}
{% endblock %}

{% block body %}
    <h1> Modifier la catégory : {{ category.name }}</h1>

    {{ form_start(formView) }}
    {{ form_errors(formView) }}
    {{ form_widget(formView) }}
    <button class="btn btn-info mt-2">Modifier la catégorie</button>
	{{ form_end(formView)}}
{% endblock %}